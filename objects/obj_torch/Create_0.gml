/// @description

var light=instance_create_layer(x,y,layer,obj_lightcircle);
light.colour=make_colour_rgb(228,98,16); //NES orange

depth+=1; //They're on the objects layer but this makes sure they're behind everything